program mc
    implicit none

    integer :: i, number_of_particles, number_of_steps, escaped_particles, current_step
    real :: material_thickness, travel_distance, particle_position, theta, pi

    pi = atan(1.0)*4.0

    number_of_particles = 100
    number_of_steps = 6
    material_thickness = 15.0
    travel_distance = 5.0
    escaped_particles = 0

    do i = 1, number_of_particles

    end do

end program